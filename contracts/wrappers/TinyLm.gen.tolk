// Auto-generated wrapper for contract 'TinyLm'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "@contracts/types"

/// Storage `Storage` is defined in `@contracts/types`
struct TinyLm {
    address: address
    stateInit: ContractState? = null
}

/// Creates a contract wrapper instance from the storage data
fun TinyLm.fromStorage(storage: Storage, toShard: AddressShardingOptions? = null): TinyLm {
    val stateInit = ContractState {
        code: build("TinyLm"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { stateInit, toShard }.calculateAddress();
    return TinyLm { address, stateInit };
}

/// Creates a contract wrapper instance from the address
fun TinyLm.fromAddress(address: address): TinyLm {
    return TinyLm { address };
}

/// Deploys the contract to the blockchain
fun TinyLm.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

fun TinyLm.sendSetModelMeta(
    self,
    from: address,
    modelVersion: uint32,
    vocabSize: uint16,
    maxContext: uint8,
    maxGenerate: uint8,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetModelMeta { modelVersion, vocabSize, maxContext, maxGenerate },
    });
    return net.send(from, genericMsg);
}

fun TinyLm.sendSetToken(
    self,
    from: address,
    tokenId: uint16,
    tokenData: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetToken { tokenId, tokenData },
    });
    return net.send(from, genericMsg);
}

fun TinyLm.sendSetTransition(
    self,
    from: address,
    previousToken: uint16,
    nextToken: uint16,
    score: int16,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetTransition { previousToken, nextToken, score },
    });
    return net.send(from, genericMsg);
}

fun TinyLm.sendClearTransition(
    self,
    from: address,
    previousToken: uint16,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ClearTransition { previousToken },
    });
    return net.send(from, genericMsg);
}

fun TinyLm.sendSetKnownReply(
    self,
    from: address,
    promptHash: uint256,
    replyBody: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetKnownReply { promptHash, replyBody },
    });
    return net.send(from, genericMsg);
}

fun TinyLm.sendClearKnownReply(
    self,
    from: address,
    promptHash: uint256,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ClearKnownReply { promptHash },
    });
    return net.send(from, genericMsg);
}

fun TinyLm.sendSetPaused(
    self,
    from: address,
    isPaused: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetPaused { isPaused },
    });
    return net.send(from, genericMsg);
}

fun TinyLm.sendWithdraw(
    self,
    from: address,
    to: address,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Withdraw { to, amount },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun TinyLm.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun TinyLm.owner(self): address {
    return net.runGetMethod(self.address, "owner");
}

fun TinyLm.config(self): TinyLmConfig {
    return net.runGetMethod(self.address, "config");
}

fun TinyLm.token(self, tokenId: uint16): TokenLookup {
    return net.runGetMethod(self.address, "token", [tokenId]);
}

fun TinyLm.transition(self, previousToken: uint16): TransitionLookup {
    return net.runGetMethod(self.address, "transition", [previousToken]);
}

fun TinyLm.neuralEmbedding(self, tokenId: uint16): NeuralEmbeddingLookup {
    return net.runGetMethod(self.address, "neuralEmbedding", [tokenId]);
}

fun TinyLm.neuralHead(self, tokenId: uint16): NeuralHeadLookup {
    return net.runGetMethod(self.address, "neuralHead", [tokenId]);
}

fun TinyLm.neuralPredictNext(self, previousToken: uint16): uint16 {
    return net.runGetMethod(self.address, "neuralPredictNext", [previousToken]);
}

fun TinyLm.predictNext(self, previousToken: uint16): uint16 {
    return net.runGetMethod(self.address, "predictNext", [previousToken]);
}
